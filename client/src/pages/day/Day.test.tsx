import { render } from 'test-utils'
import Day from "."

describe('Day', () => {
        test('should have welcome message with users name', () => {
                render(<Day />)
                
        })
})